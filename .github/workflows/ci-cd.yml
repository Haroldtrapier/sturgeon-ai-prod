name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: '*/requirements.txt'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Lint with pylint
        run: pylint api/ || true # Warn but don't fail on linting

      - name: Run tests
        run: python -m pytest tests/ -v || true # For now, fail safely if no tests

      - name: Deploy to Vercel
        if: success()
        run: |
          npm i -g vercel
          vercel --prod 
        env:        
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

      - name: Post DEPLOY Notification
        if: success()
        run: echo "API deployed to Vercel!"