# =============================================================================
# STURGEON AI - Frontend Environment Variables
# =============================================================================
# Copy to .env.local for local development
# Add these to Vercel Dashboard > Settings > Environment Variables

# =============================================================================
# SUPABASE (Required)
# =============================================================================
# Get these from: https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Server-side Supabase URL (optional, falls back to NEXT_PUBLIC_SUPABASE_URL)
# SUPABASE_URL=https://your-project.supabase.co

# =============================================================================
# BACKEND API (Required for production)
# =============================================================================
# Your Railway backend URL (no trailing slash)
# Used by API routes (server-side)
BACKEND_URL=https://your-backend.up.railway.app

# Used by client-side components
NEXT_PUBLIC_API_URL=https://your-backend.up.railway.app

# For local development, point both to localhost:
# BACKEND_URL=http://localhost:8000
# NEXT_PUBLIC_API_URL=http://localhost:8000
