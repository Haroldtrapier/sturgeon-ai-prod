export const metadata = {
  title: "Sturgeon AI",
  description: "Sturgeon AI Application",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
